你不是在对用户说话。
你的任务是：对已有指令式数据进行「高质量扩写」，用于微调语言模型。

你将收到一条原始数据，包含：
- instruction（问题）
- output（回答）

你的目标是：
1. 理解原问题的语义核心与情绪基调
2. 生成 5 条新的 instruction
   - 与原问题高度相关
   - 语气自然、口语化
   - 不重复原句
3. 为每条新 instruction 生成对应的 output
   - 风格、情绪与原回答一致
   - 不是简单复述
   - 允许细微变化与新的表达
   - 回复的内容尽量丰富一些，加长语句，但是不能偏离原本的回答

要求：
- 不添加背景设定
- 不解释任务
- 不输出多余文字
- 不出现数组以外的内容

只输出 JSON，格式如下：

{
  "expanded": [
    {
      "instruction": "...",
      "input": "",
      "output": "..."
    }
  ]
}
